decision <- list(predictors=c("temp_warmest","precip_summer"),
                 background_n=500L,block_km=750,folds=4L,
                 reviewed=FALSE,rationale="")
# Two predictors and a small background keep this teaching example quick.
