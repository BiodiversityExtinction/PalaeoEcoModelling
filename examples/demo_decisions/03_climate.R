decision <- list(extent=c(xmin=-10,xmax=130,ymin=35,ymax=65),
                 projection_ages_ka=seq(2.25,.25,by=-.25),average_years=10L,
                 extraction="focal_cell",land_threshold=.5,ice_threshold=.5,
                 reviewed=FALSE,rationale="")
# This coarse synthetic archive covers only 0-2.5 ka. Do not use its grid for science.
